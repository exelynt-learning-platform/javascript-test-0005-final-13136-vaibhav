This program prints a triangle pattern using numbers.

A variable num is used to store the number to be printed.

The outer loop controls the rows.

The inner loop prints numbers in each row and increases the value of num after every print.

This creates a triangle where numbers continue increasing in each row.
